<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app = "http://schemas.android.com/apk/res-auto"
    xmlns:tools = "http://schemas.android.com/tools"
    tools:context = ".MainActivity">

    <RelativeLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingLeft="@dimen/activity_horizontal_margin"
        android:paddingRight="@dimen/activity_horizontal_margin"
        android:paddingTop="@dimen/activity_vertical_margin"
        android:paddingBottom="@dimen/activity_vertical_margin"

        tools:showIn="@layout/age_selector"
        tools:context=".HomeActivity"></RelativeLayout>


    <item android:id="@+id/menu_main"
        android:title="Menu"
        android:orderInCategory="100"
        android:showAsAction="always" >

        <menu>
            <item
                android:id="@+id/new_age"
                android:orderInCategory="100"
                android:title="New Age"/>
            <item
                android:id="@+id/load_age"
                android:orderInCategory="100"
                android:title="Load Age"/>
            <item
                android:id="@+id/save_age"
                android:orderInCategory="100"
                android:title="Save Age"/>
            <!--- android:onClick="OpenNextActitvity"/>
            -->
        </menu>

    </item>

</menu>
